<h1>Organisations</h1>

<%= link_to 'New organisation', new_organisation_path %>

<% @organisations.each do |organisation| %>
  <ul>
    <li><%= organisation.name %> <%= link_to 'Edit', edit_organisation_path(organisation) %></li>
  </ul>
<% end %>